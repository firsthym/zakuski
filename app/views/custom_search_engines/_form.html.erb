<%= form_for(@custom_search_engine) do |f| %>
  <% if @custom_search_engine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@custom_search_engine.errors.count, "error") %> prohibited this custom_search_engine from being saved:</h2>

      <ul>
      <% @custom_search_engine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
