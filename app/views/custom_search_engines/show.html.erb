<div class="container-fluid">
	<div class="row-fluid">
		<div class="span8 offset2">
			<div class="row-fluid">
				<%= render 'nodes/breadcrumb' %>
			</div>
			<div class="row-fluid">
				<div class="well">
				<h3><%= @custom_search_engine.specification.title %></h3>
				<div class="row-fluid">
					<div class="cse-grid"><%= link_to @custom_search_engine.author.username, user_path(@custom_search_engine.author) %><span class="divider">•</span><span class="muted"><%=  time_ago_in_words(@custom_search_engine.created_at)%></span>
					</div>
					<div class="cse-grid">
						<blockquote>
  							<p><%= @custom_search_engine.specification.description %></p>
						</blockquote>
					</div>
					<div class="row-fluid">
						<div class="cse-box">
							<div class="row-fluid cse-grid">
								<% if @filter_annotations.any? %>
									<h5><strong><%= I18n.t('human.text.mode_filter') %></strong></h5>
									<% @filter_annotations.each do |annotation| %>
									<p><%= annotation.about %></p>
									<% end %>
								<% end %>
							</div>
							<div class="row-fluid cse-grid">
								<% if @exclude_annotations.any? %>
									<h5><strong><%= I18n.t('human.text.mode_exclude') %></strong></h5>
									<% @exclude_annotations.each do |annotation| %>
									<p><%= annotation.about %></p>
									<% end %>
								<% end %>
							</div>
							<div class="row-fluid cse-grid">
								<% if @boost_annotations.any? %>
									<h5><strong><%= I18n.t('human.text.mode_boost') %></strong></h5>
									<% @boost_annotations.each do |annotation| %>
									<p><%= annotation.about %></p>
									<% end %>
								<% end %>
							</div>
							<div class="row-fluid cse-grid">
								<div class="span6 offset3"><%= link_to I18n.t('human.text.link_current_cse'), cse_link_path(@custom_search_engine), confirm: 'are you sure?', remote: true, class: 'btn btn-block' %></div>
							</div>
							<% unless @custom_search_engine.author == current_user %>
							<div class="row-fluid cse-grid">
								<div class="span6 offset3"><a href="javascript:void(0)" class="btn btn-block"><%= I18n.t('human.text.get_current_cse') %></a></div>
							</div>
							<% end %>
						</div>
					</div>
				</div>
				</div>				
			</div>
		</div>
	</div>
</div>
