<div class="row-fluid">
	<div>
		<div class="row-fluid">
			<%= link_to I18n.t('human.controls.link.new_cse'), new_node_cse_path(@selected_node), class: 'btn' %>
		</div>
		<div class="row-fluid cse-grid">
			<% if @custom_search_engines.any? %>
				<% custom_search_engines = @custom_search_engines.page(params[:page]) %>
				<% custom_search_engines.each do |cse| %>
					<% unless cse.access == 'private' %>
						<div class="cse-divider"></div>
						<h5>
						<%= get_access_html(cse) %>
						<%= get_link_count_html(cse) %>
						<%= link_to cse.specification.title, cse_path(cse) %>
						</h5>
						<div class="cse-grid">
							<div><%= link_to cse.author.username, user_path(cse.author) %><span class="divider">•</span><span class="muted"><%=  time_ago_in_words(cse.created_at)%></span>
							</div>
						</div>
					<% end %>
				<% end %>
				<div class="row-fluid">
					<div class="pull-right">
					<%= paginate custom_search_engines %>
					</div>
				</div>
			<% else %>
			<div class="cse-grid">
				<blockquote>
				  <h5><%= I18n.t('human.text.no_cse_yet') %></h5>
				  <small><%= I18n.t('human.text.why_create_cse') %></small>
				</blockquote>
			</div>
			<% end %>		
		</div>
	</div>
</div>