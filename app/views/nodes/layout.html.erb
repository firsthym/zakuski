<div class="container">
	<div class="row">
		<div class="span7 offset1">
			<div class="cse-box-padding well">
			<div class="row-fluid">
				<%= render 'nodes/header' %>
			</div>
			<div class="row-fluid">
				<% if params[:post_type] == 'cses' %>
				<% @custom_search_engines = @posts %>
					<%= render 'nodes/cse_body' %>
				<% elsif params[:post_type] == 'topics' %>
					<%= render 'topics/topic_body' %>
				<% end %>
			</div>
			<div class="row-fluid">
				<%= render 'nodes/footer' %>
			</div>
			</div>
		</div>
		<div class="span3">
			<%= render 'shared/dashboard' %>
		</div>
	</div>
</div>
