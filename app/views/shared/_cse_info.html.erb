<%= link_to truncate(cse.title, length: 15), cse_path(cse), title: cse.title %><span class="divider">•</span><%= get_link_count_html(cse) %>
<% if cse.status == 'draft' %>
	<span class="divider">•</span><span class="label"><%= t('human.text.unshared') %></span>
<% end %>
