<div class="cse-divider"></div>
<div class="row-fluid">
	<div class="span10 offset2">
		<div class="row-fluid">
			<%= link_to reply.user.username, reply.user %><span class="divider">•</span><span class="muted"><%=  time_ago_in_words(reply.created_at)%></span><span class="badge badge-important pull-right"><%= reply.index %></span>
		</div>
		<div class="row-fluid cse-grid">
			<p class="cse-word"><%= reply.body %></p>
		</div>
	</div>
</div>